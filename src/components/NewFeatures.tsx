const NewFeatures = ({ imgUrl, title, subtitle }) => (
  <div className='flex min-w-[210px] flex-1 flex-col sm:max-w-[600px]'>
    <p className='mt-[0px] flex-1 text-[18px] font-normal leading-[32.4px] text-[#B0B0B0]'>
      {subtitle}
    </p>
  </div>
);

export default NewFeatures;
